To use this application on anything, make sure you have python installed on your computer. Then open the terminal/command line/shell, in the command line, type cd and drag the folder into the terminal and hit enter. type python3 repeat.py and press enter. Repeat should start running in the command line.